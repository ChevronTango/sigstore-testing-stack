# Set it to a valid URL
issuer: http://localhost:5556

# See https://dexidp.io/docs/storage/ for more options
storage:
  type: memory

staticClients:
  - id: sigstore
    name: 'Sigstore'
    public: true

connectors:
  - type: mockCallback
    id: mock
    name: Example
